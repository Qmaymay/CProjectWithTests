@echo off
echo ?? ??????...
if not exist lib\libcalculator.dll (
    echo ? ??: ??? libcalculator.dll
    echo ?? ???? build.bat
    pause
    exit /b 1
)
cd calculator_tests
python test_interfaces.py
if %errorlevel% equ 0 (
    echo ? ???????
) else (
    echo ? ????
)
pause
