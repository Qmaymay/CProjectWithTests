name: C Calculator CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential

      - name: Install build dependencies (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          choco install cmake -y

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Build Project (Windows)
        if: matrix.os == 'windows-latest'
        shell: cmd
        run: |
          echo "=== Windows 构建 ==="

          @REM 清理旧的构建目录
          if exist build rmdir /s /q build
          if exist lib rmdir /s /q lib

          mkdir build
          cd build
          cmake ..
          cmake --build . --config Release
          cd ..

          echo "=== 处理构建产物 ==="
          mkdir lib

          @REM 从多个可能的位置复制DLL
          copy build\calculator\Release\*.dll lib\ 2>nul || echo "尝试位置1: 无DLL"
          copy build\Release\*.dll lib\ 2>nul || echo "尝试位置2: 无DLL"
          copy build\*\Release\*.dll lib\ 2>nul || echo "尝试位置3: 无DLL"

      - name: Build Project (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          echo "=== Linux 构建 ==="

          # 清理旧的构建目录
          if [ -d "build" ]; then
            rm -rf build
          fi
          if [ -d "lib" ]; then
            rm -rf lib
          fi

          mkdir build && cd build
          cmake ..
          cmake --build . --config Release
          cd ..

          echo "=== 处理构建产物 ==="
          mkdir -p lib
          find build -name "*.so" -exec cp {} lib/ \; 2>/dev/null || echo "无SO文件"
      - name: Run Python interface tests
        run: |
          cd calculator_tests
          python test_interfaces.py --verbose
        env:
          PYTHONIOENCODING: utf-8
